spring:
  data:
    jpa:
      repositories:
        enabled: true
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:54.197.8.130}:32699/postgresdb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
  flyway:
    baseline-on-migrate: true
    out-of-order: true

server:
  port: 8090

logging:
  level:
    org:
      springframework:
        web: DEBUG
